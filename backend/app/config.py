"""
รวมค่าตั้งศูนย์กลางของโปรเจกต์
เวลาอยากเปลี่ยนโมเดล แก้ที่เดียวที่นี่ ไม่ต้องไล่แก้ทั้งโปรเจกต์
"""

# URL ของ Ollama (เครื่องยนต์ AI ที่รันอยู่ในเครื่อง)
OLLAMA_URL = "http://localhost:11434"

# โมเดลหลักที่ใช้คุย — เปลี่ยนเป็นตัวอื่นที่ `ollama list` เจอได้
# ตัวอย่าง: "llama3.2", "qwen3:4b", "gemma3:4b"
LLM_MODEL = "llama3.2"

# โมเดลสำหรับทำ embedding (แปลงข้อความเป็นเวกเตอร์ตัวเลข)
# ใช้ในระบบ RAG (ถาม-ตอบจากเอกสาร) ที่จะสร้างใน Phase 3
EMBEDDING_MODEL = "nomic-embed-text"
